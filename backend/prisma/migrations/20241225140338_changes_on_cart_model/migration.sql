BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Cart] ADD CONSTRAINT [Cart_Quantity_df] DEFAULT 1 FOR [Quantity];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
